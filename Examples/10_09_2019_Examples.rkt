#lang racket


;(define n 100)
;(foldl + 0 (range n))

(let ((a 2) (b 3)) (+ a b))

;Missed this one look in slides ( a +)

;(letrec )

;(define-syntax-rule (if-335 condition then a else b)
 ; (if condition a b)
;)

;(if-335 (> 3 2) then "right!" else "wrong!")